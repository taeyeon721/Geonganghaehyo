<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.main.Message.dao.MessageMapper">

    <select id="userLastMsg" resultType="MessageDto">
        select * from message where email=#{email} and user_id=#{userId} and isSound == 'false' order by time desc limit 1
    </select>

    <select id="managerLastMsg" resultType="MessageDto">
        select * from message where email=#{email} and user_id=#{userId} and is_sound == 'true' order by time desc limit 1
    </select>

    <select id="msgList" resultType="MessageDto">
        select * from from message where email=#{email} and userId=#{userId}
    </select>

    <insert id="insertMsg" parameterType="MessageDto"> <!-- uuid 설정 필요 -->
        insert into message(email, user_id, is_sound, content, time) values(#{email}, #{userId}, #{isSound}, #{content}, #{time})
    </insert>

</mapper>